package ${(genCodeRecord.packageName)!""}.${(genCodeRecord.moduleName)!""}.service;

import com.baomidou.mybatisplus.extension.service.IService;
import ${(genCodeRecord.packageName)!""}.${(genCodeRecord.moduleName)!""}.entity.${(tableInfo.clazzName)!""};

/**
 * @author ${(genCodeRecord.moduleAuthor)!""}
 * @Description: ${(genCodeRecord.moduleDesc)!""} Service接口
 * @Date: ${(currDate)!""}
 */

public interface ${(tableInfo.clazzName)!""}Service extends IService<${(tableInfo.clazzName)!""}> {

}
